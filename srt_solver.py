#! /usr/bin/python

# Catenaries are (a * Cosh[x/a]).
# The integral is (a^2 * Sinh[x/a]).
# The arc length integral is (a*Sqrt(Cosh(x/a)^2)*Tanh(x/a)).

def get_catenary_parameters(x0, y0, x1, y1, length):
	pass

def energy_in_rope(x):
	pass

class RopePoint:
	def __init__(self):
		pass

